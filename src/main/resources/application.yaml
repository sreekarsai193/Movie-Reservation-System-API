spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/movie
    username: postgres
    password: tawkenskiy
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true